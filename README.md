Run using Python 3.10, dependencies listed in requirements.txt.

Dataset is the HI-Small_accounts.csv from https://www.kaggle.com/datasets/ealtman2019/ibm-transactions-for-anti-money-laundering-aml

Note the code works without CUDA installed since it uses the CPU for training, however changing the device is possible in the codebase.
